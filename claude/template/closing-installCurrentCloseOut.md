Something unexpected happened it seems, and there may be something wrong with the test repo. 
Be sure to look into the files from `map` folder, `install.bat`, and if the `sandbox` folder
still exist, then check it to. Create a new branch. Call `install-check`, make edits, and
commit to `install-check`.
